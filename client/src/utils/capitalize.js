/**
 * Returns the string with an uppercase letter at the start
 * @param {string} str 
 * @returns string
 */
const capitalize = (str) => str.charAt(0).toUpperCase() + str.slice(1);

export default capitalize